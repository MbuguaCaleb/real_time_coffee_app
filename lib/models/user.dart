class User {
  final String uid;

/*constructor function that automatically applies uid as named parameter*/
/*Remember that it is good practice for the constructor having the same name
as the class*/

  User({this.uid});
}
